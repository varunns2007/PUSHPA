Place an optional licensed cinematic intro video here as "intro.mp4".
If this file is not present, PUSHPA automatically falls back to its
built-in CSS/WebGL cinematic intro (this is the current default —
no video is bundled with this project).
